# AegisScan backend dependencies
# Pinned to ranges that are known-compatible with Python 3.11 - 3.13.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0

# Database drivers: SQLite ships with Python. psycopg (v3) is only needed for Postgres.
# psycopg v3 (NOT psycopg2) has wheels for Python 3.13 - this is a deliberate fix
# for the exact driver problem PostureScan hit on its slide 9/10.
psycopg[binary]==3.2.3

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Probing / scanning
httpx==0.28.1
dnspython==2.7.0

# Reporting
Jinja2==3.1.5
reportlab==4.2.5          # pure-python PDF, no system libraries required

# Observability (optional, no-op unless SENTRY_DSN is set)
sentry-sdk==2.61.0        # version that fixes the silent recursion crash on 3.13

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
